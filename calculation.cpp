#include<iostream>
using namespace std;
int main ( )
{
    int a,b,c,res;
    char s;
    cin >> a >> b >> c >> s ;

    if ( (s = '+' && a+b) && (s = '-' && a-b) && (s = '*' && a*b) )
    {
        cout << "Yes" << endl;
    }
    else
    {
        cout << " "<< ;
    }
    return 0;
}
